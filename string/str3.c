#include<stdio.h>
#include<string.h>
void main()
{
    char str1[50], str2[50];
    int i,j,k=0;
    printf("Enter the Name: ");
    scanf("%[^\n]s",&str1);

    printf("Original Name: %s",str1);

    for(i=0;str1[i]!='\0';i++);
    {
        if(i=0 && i='\0'-1)
        {
            printf("%s",i);
        }
        else if(i=1 &&)
    }
    
}